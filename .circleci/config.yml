version: 2
jobs:
   build:
     docker:
       - image: circleci/golang:1.8.3
     steps:
       - run: hostname
       - run: echo "hello - build"
   test:
     docker:
       - image: circleci/golang:1.8.3
     steps:
       - run: hostname
       - run: echo "hello - test"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test
